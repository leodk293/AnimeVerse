import React from 'react'

export const metadata = {
    title: "Searching-AnimeVerse",
    description: "Users an search an aime name here...",
};

export default function layout({ children }) {
    return (
        <div>{children}</div>
    )
}
