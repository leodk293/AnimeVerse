import React from 'react'

export const metadata = {
    title: "Popular-AnimeVerse",
    description: "Popular will be displayed here...",
};

export default function layout({ children }) {
    return (
        <div>{children}</div>
    )
}
