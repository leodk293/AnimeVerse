import React from 'react'

export const metadata = {
  title: "Airing-AnimeVerse",
  description: "This is the page where anime on air are displayed",
};

export default function layout({ children }) {
  return (
    <div>{children}</div>
  )
}
