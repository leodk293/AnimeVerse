import React from 'react'

export const metadata = {
    title: "Upcoming-AnimeVerse",
    description: "Here, Anime Upcoming are displayed",
};

export default function layout({ children }) {
    return (
        <div>{children}</div>
    )
}
